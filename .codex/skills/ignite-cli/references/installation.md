# CLI installation

## Build and install from source
From an Ignite source checkout:
- `make`
- `make install`

The install step places the CLI and `ignite-server.py` under `/usr/local/bin`.

## Preview note
The recommended way to preview a site locally is `ignite run --preview`.
